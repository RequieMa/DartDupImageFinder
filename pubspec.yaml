name: dup_image_finder
description: A cross-platform pure Dart library for detecting duplicate images using various methods (e.g. hashing, CNN), supporting multiple image formats and efficient similarity comparison
version: 0.1.0
repository: https://github.com/RequieMa/dup_image_finder
license: BSD-3-Clause
# documentation: https://pub.dev/documentation/your_package_name # 自动生成
issue_tracker: https://github.com/RequieMa/dup_image_finder/issues

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  image: ^4.5.3
  # collection: ^1.19.1
  path: ^1.8.0
  logging: ^1.3.0
  bk_tree: ^0.1.1
  image_hashing: ^0.1.0
  # dartcv4: ^1.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

topics: # 最多5个标签
  - photos
  - machine-learning
  - duplicates-detection
  - images
  - dart
