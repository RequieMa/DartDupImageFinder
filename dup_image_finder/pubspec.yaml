name: dup_image_finder
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  image: ^4.5.3
  collection: ^1.19.1
  # worker_pool: ^3.0.0
  # progress: ^1.9.0
  path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
